{
  "name": "shareholder-app",
  "version": "1.0.0",
  "private": true,
  "description": "Android app for shareholders - live figures and the chatbot from the accounting system",
  "scripts": {
    "add:android": "cap add android",
    "sync": "cap sync android",
    "open": "cap open android",
    "build:apk": "cd android && gradlew.bat assembleDebug"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  }
}
